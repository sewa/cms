<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "CMS" %></title>

    <%= stylesheet_link_tag    "cms/application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "cms/application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1>
            <%= link_to t(:cms), content_nodes_path %>
          </h1>
        </li>
      </ul>
      <section class="top-bar-section">
        <ul class="right">
          <li class="has-form">
            <%= link_to t(:logout), main_app.destroy_cms_user_session_path, method: :delete, class: 'button radius' %>
          </li>
        </ul>
        <ul class="left">
          <%= nav_link_to t(:pages), content_nodes_path, object: :content_nodes %>
          <%= nav_link_to t(:images), content_images_path, object: :content_images %>
          <%= nav_link_to t(:documents), content_documents_path, object: :content_documents %>
          <%= nav_link_to t(:categories), content_categories_path, object: :content_categories %>
        </ul>
      </section>
    </nav>

    <%= render 'cms/shared/breadcrumb' %>

    <%= yield :actions %>

    <%= yield %>

  </body>
</html>
