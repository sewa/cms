<table width="100%">
  <thead>
    <tr>
      <th><%= t("name") %></th>
      <th><%= t("description") %></th>
      <th><%= t("keywords") %></th>
      <th width="243" class="actions"></th>
    </tr>
  </thead>
  <tbody>
    <% objects.each do |category| %>
      <tr id="<%= dom_id category %>">
        <td><%= category.name %></td>
        <td><%= category.description %></td>
        <td><%= category.keywords %></td>
        <td class="actions">
          <ul class="button-group right radius">
            <li>
              <%= link_to t(:edit), edit_content_category_path(category), class: 'button small'  %>
            </li>
            <li>
              <%= link_to t(:delete), content_category_path(category), 'data-confirm': t(:really_delete), method: :delete, class: 'button small' %>
            </li>
          </ul>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
